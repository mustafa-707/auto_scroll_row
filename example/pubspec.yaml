name: example
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  auto_scroll_row:
    path: "../"

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
